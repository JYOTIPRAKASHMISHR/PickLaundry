<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base Material3 Theme -->
    <style name="Base.Theme.Picklaundry" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Explicit color overrides -->
        <item name="colorPrimary">@color/my_primary</item>
        <item name="colorOnPrimary">@color/my_on_primary</item>
        <item name="colorPrimaryContainer">@color/my_primary_container</item>
        <item name="colorOnPrimaryContainer">@color/my_on_primary_container</item>

        <!-- Disable dynamic colors if needed -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

    <style name="Theme.Picklaundry" parent="Base.Theme.Picklaundry" />
</resources>
